# Copy this file to config.yaml and replace every <...> placeholder.
# config.yaml is intentionally excluded from Git.

DASH_SSH_ALIAS: "<DASH_SSH_ALIAS>"
REMOTE_GIT_BARE: "<REMOTE_GIT_BARE>"
REMOTE_APP_DIR: "<REMOTE_APP_DIR>"
REMOTE_DATA_ROOT: "<REMOTE_DATA_ROOT>"
REMOTE_DB_PATH: "<REMOTE_DB_PATH>"
REMOTE_MEDIA_ROOT: "<REMOTE_MEDIA_ROOT>"
REMOTE_INCOMING_ROOT: "<REMOTE_INCOMING_ROOT>"
REMOTE_BACKUP_ROOT: "<REMOTE_BACKUP_ROOT>"
SERVER_VENV: "<SERVER_VENV>"

# Fixed local layout:
#   <LOCAL_STAGE_ROOT>/ytcrawl.sqlite3
#   <LOCAL_STAGE_ROOT>/media/
LOCAL_STAGE_ROOT: "<LOCAL_STAGE_ROOT>"
REVIEW_HOST: "0.0.0.0"
REVIEW_PORT: 8765
